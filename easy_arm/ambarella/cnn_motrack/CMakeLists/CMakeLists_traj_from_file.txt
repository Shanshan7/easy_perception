#1.cmake verson
cmake_minimum_required(VERSION 3.5.0)

#2.project name
project(cnn_motrack)

set(CMAKE_ASM_COMPILER ${CMAKE_C_COMPILER})

#add_definitions(-DIS_SHOW)
#add_definitions(-DIS_SEND_DATA)

#include amba_arm directories
include(${PROJECT_SOURCE_DIR}/../cmake/ambacv25_3_dependencies.cmake)

#include Calculate traj headers
include_directories(${PROJECT_SOURCE_DIR}/demo_traj/include)

#link cal_traj.so
link_directories(${PROJECT_SOURCE_DIR}/demo_traj/lib)

add_executable(test_traj_from_file "")

target_sources(test_traj_from_file
        PRIVATE
        "${PROJECT_SOURCE_DIR}/test_traj_from_file.cpp"
        )

# link libraries
target_link_libraries(test_traj_from_file
                      cal_traj
                      opencv_core
                      opencv_imgcodecs
                      opencv_imgproc
                      opencv_videoio
                      tbb
                      jpeg
                      png
                      z
                      bz2
                      freetype
                      pthread)
